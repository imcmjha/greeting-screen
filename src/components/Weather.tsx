import * as React from 'react';

export class Weather extends React.Component<{}, {}> {
  public render() {
    return (
      <React.Fragment>
        We suppose u r staying in Pune and it is chilling heat out there. stay inside.
      </React.Fragment>
    );
  }
}

export default Weather;